import { IfAuthentificationDirective } from './if-authentification.directive';

describe('IfAuthentificationDirective', () => {
  it('should create an instance', () => {
    const directive = new IfAuthentificationDirective();
    expect(directive).toBeTruthy();
  });
});
